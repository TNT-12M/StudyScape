# 题目-答案自动匹配功能说明

## 一、功能概述

解决"试卷和答案是分开的两份文件"的场景：上传题目文件和答案文件后，系统会**自动匹配**并将答案回填到对应题目中。

- **不限顺序**：先上传题目还是先上传答案都可以
- **自动匹配**：基于文件名生成"匹配码"，自动识别同一套卷的题目和答案
- **自动回填**：匹配成功后，答案自动按题号回填到题目中
- **批量管理**：可查看所有导入批次的匹配状态

---

## 二、匹配原理

### 2.1 匹配码（match_key）

系统从文件名中提取核心标识作为匹配码，规则如下：

1. 去掉扩展名
2. 去掉以下干扰词：
   - 试卷类：试卷、试题、考题、题目、练习、训练、真题、模拟卷、模拟题、冲刺卷、预测卷、押题卷
   - 答案类：答案、解析、解答、参考答案、答案及解析、真题及答案、真题答案、及答案、及解析、含答案、含解析
3. 去掉空格、横线、括号等符号
4. 转小写归一化

**示例**：

| 文件名 | 提取后的匹配码 |
|--------|--------------|
| `2025年中考安徽生物真题试卷.pdf` | `2025年中考安徽生物` |
| `2025年中考安徽生物真题及答案.pdf` | `2025年中考安徽生物` |
| `高一化学-期中测试卷.docx` | `高一化学期中测试` |
| `高一化学-期中测试-答案.docx` | `高一化学期中测试` |

> **匹配成功的关键**：题目文件和答案文件的文件名**核心部分一致**，只是一个带"试卷"一个带"答案"。

### 2.2 给 PaperCutter-VL / OCR流水线 的建议

如果输出的 JSON 中能加入明确的匹配标识，匹配会更准确：

**推荐方案**：在 JSON 输出中加入 `match_key` 字段

```json
{
  "match_key": "2025_anhui_zhongkao_biology",
  "questions": [...]
}
```

或者**文件名规范**：
- 题目文件：`{标识}_试卷.pdf` 或 `{标识}_questions.pdf`
- 答案文件：`{标识}_答案.pdf` 或 `{标识}_answers.pdf`

其中 `{标识}` 是同一套卷的唯一标识（如 `2025anhui_bio`）。

---

## 三、使用方法

### 3.1 操作步骤

**场景一：先上传题目，再上传答案**

1. 进入「题库中心」→「文档智能导入」
2. 选择「📝 题目文件」模式
3. 上传题目文件（PDF/Word/TXT），点击「智能解析」
4. 解析完成后，系统会提示"暂未找到匹配的答案文件"，并显示匹配码
5. 切换到「✅ 答案文件」模式
6. 上传对应的答案文件，点击「智能解析」
7. 系统自动匹配并回填答案，提示"自动匹配成功"
8. 回到「📝 题目文件」模式（或直接看 JSON 文本框），确认答案已回填
9. 选择学段后点击「开始导入」入库

**场景二：先上传答案，再上传题目**

1. 选择「✅ 答案文件」模式，上传答案文件
2. 系统提示"暂未找到匹配的题目文件"
3. 切换到「📝 题目文件」模式，上传题目文件
4. 系统自动匹配并回填答案
5. 确认后导入入库

### 3.2 查看匹配状态

点击「📋 查看匹配状态」按钮，可查看所有导入批次：

| 状态 | 说明 |
|------|------|
| ⏳ 待匹配 | 上传了但还没找到对应的另一半 |
| ✅ 已匹配 | 题目和答案已成功配对 |
| 📥 已入库 | 已经导入到题库中 |

---

## 四、答案解析规则

系统支持以下答案格式的自动识别（OCR 后自动解析）：

### 格式1：区间格式（最常见）
```
1-5: DBCCB
6-10: ACABD
11-15: BCDAE
```

### 格式2：单行格式
```
1. A
2. B
3. C
```

### 格式3：内联格式
```
1 A 2 B 3 C 4 D
```

解析后存储为 `{题号: 答案}` 的格式，按题号与题目匹配。

---

## 五、API 接口

| 接口 | action | 说明 |
|------|--------|------|
| 题目文件导入 | `extract_document` | 解析题目，存入批次，自动查找匹配的答案 |
| 答案文件导入 | `extract_answers` | 解析答案，存入批次，自动查找匹配的题目 |
| 批次列表 | `import_batch_list` | 获取所有导入批次及匹配状态 |
| 手动匹配 | `import_match_batches` | 手动指定两个批次匹配 |
| 确认入库 | `import_commit_batch` | 将匹配好的批次正式入库 |

---

## 六、数据库表结构

### import_batches 表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER | 主键 |
| `match_key` | TEXT | 匹配码（核心标识） |
| `batch_type` | TEXT | `questions`=题目 / `answers`=答案 |
| `file_name` | TEXT | 原始文件名 |
| `subject` | TEXT | 科目 |
| `education_level` | TEXT | 学段 |
| `question_count` | INTEGER | 题目数量 |
| `answer_count` | INTEGER | 答案数量 |
| `questions_json` | TEXT | 题目数据 JSON |
| `answers_json` | TEXT | 答案数据 JSON（`{题号: 答案}`） |
| `matched_batch_id` | INTEGER | 匹配到的对方批次 ID |
| `match_status` | TEXT | `pending` 待匹配 / `matched` 已匹配 / `used` 已入库 |
| `created_by` | INTEGER | 创建人用户 ID |
| `created_at` | DATETIME | 创建时间 |
| `updated_at` | DATETIME | 更新时间 |

### questions 表（新增字段）

| 字段 | 类型 | 说明 |
|------|------|------|
| `match_key` | TEXT | 匹配码（同一套卷的唯一标识） |
| `source_qid` | TEXT | 原始题号（配合 match_key 做精准匹配） |

---

## 七、与 OCR 流水线 / PaperCutter-VL 对接建议

### 7.1 输出格式建议

**题目文件 JSON**（`questions` 数组，每个元素含题号）：

```json
{
  "match_key": "2025_anhui_zhongkao_biology",
  "paper_name": "2025年中考安徽生物真题试卷",
  "subject": "生物",
  "education_level": "junior",
  "questions": [
    {
      "question_id": "1",
      "question_type": "单选题",
      "question_content": "题干内容...",
      "question_options": ["A. 选项1", "B. 选项2", "C. 选项3", "D. 选项4"],
      "question_images": ["base64..."],
      "difficulty": 3
    }
  ]
}
```

**答案文件 JSON**：

```json
{
  "match_key": "2025_anhui_zhongkao_biology",
  "paper_name": "2025年中考安徽生物真题答案",
  "answers": {
    "1": "A",
    "2": "C",
    "3": "B",
    "4": "D"
  }
}
```

### 7.2 关键字段

| 字段 | 必要性 | 说明 |
|------|--------|------|
| `match_key` | **强烈建议** | 同一套卷的题目和答案文件使用相同的匹配码，确保 100% 准确匹配 |
| `question_id` / 题号 | **必须** | 用于答案和题目按号对应 |
| `subject` | 建议 | 科目，避免自动识别出错 |
| `education_level` | 建议 | 学段 `junior`/`senior`，避免人工选择 |
| `question_type` | 建议 | 题型（单选题/多选题/填空题等） |

### 7.3 如果用文件名匹配

如果不方便加 `match_key` 字段，请确保文件名遵循以下模式：

```
{核心标识} + 试卷/试题/题目 + .{扩展名}    ← 题目文件
{核心标识} + 答案/解析/参考答案 + .{扩展名}  ← 答案文件
```

**示例**：
- `2025安徽中考生物_试卷.pdf`
- `2025安徽中考生物_答案.pdf`

核心标识完全一致，系统就能正确匹配。

---

## 八、注意事项

1. **题号对齐**：答案是按题号和题目匹配的，请确保题目文件中的题号和答案文件中的题号一致
2. **匹配码准确性**：基于文件名的自动匹配在文件名规范的情况下准确率很高；如果文件名差异大，建议手动匹配
3. **手动匹配**：如果自动匹配失败，可在"查看匹配状态"中手动选择两个批次进行匹配
4. **查重保护**：最终入库时仍然会做查重（相同题干+答案+科目+学段视为重复）
